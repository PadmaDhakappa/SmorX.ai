# Django settings
SECRET_KEY=your-very-secret-django-key-here-change-this
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database (SQLite by default — override for PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/smorx_db

# Email (Gmail SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-gmail@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=SmorX.ai <outreach@smorx.ai>
